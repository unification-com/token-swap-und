<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta sendaddress="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css" integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
  <link rel="stylesheet" href="stylesheets/style.css">
  <title>Token swap</title>
</head>
<body>
    <form class="form" action="/" method="post">
      <h1>Swap Tokens</h1>
      <div class="form-group">
        <input type="text" placeholder=" " id=="sendaddress" name="sendaddress">
        <label for="sendaddress">Ethereum Address</label>
        <span id="note">ETH Address you will send UND ERC-20 Tokens from</span>
        <% if (error && error.sendaddress) { %>
        <div class="error">
          <i class="fas fa-exclamation-circle"></i>
          <%= error.sendaddress %>
        </div>
        <% } %>
      </div>
      <br/>
      <div class="form-group">
        <input type="text" placeholder=" " id="destaddress" name="destaddress">
        <label for="destaddress">Binance Chain Address</label>
        <span id="note">Binance Chain address that will receive the swapped tokens</span>
        <% if (error && error.destaddress) { %>
        <div class="error">
          <i class="fas fa-exclamation-circle"></i>
          <%= error.destaddress %>
        </div>
        <% } %>
      </div>
			<div class="form-group">
			<p id="note">
				Note : If you previously have indicated another destination binance chain address for tokens sent from this ethereum address , the previous swap requests will be updated with the new address
			</p>
      <p id="note">
        All token swaps  will be reviewed and processed manually , and will be final once done.
      </p>
			</div>
      <input type="submit" value="Start swap">
      <div class="form-bottom">
        <a href="/status">Check swap status</a>
      </div>
    </form>
</body>
</html>
